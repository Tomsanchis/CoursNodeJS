// 4 | Écrivez une fonction qui prend un tableau de chaînes de caractères et renvoie un tableau de chaînes de caractères triées par ordre alphabétique

export const OrdreAlphabet = (str) => {
  return str.sort();
};
